package com.example.ps.config;

public record TypeCacheConfig(  String name,
                                int capacity,
                                int count,
                                boolean enabled) {
}
